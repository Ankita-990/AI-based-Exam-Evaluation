streamlit>=1.32
pdf2image>=1.17
# CPU-only torch build — EasyOCR depends on torch, but the default PyPI wheel
# pulls in ~2GB of unused NVIDIA/CUDA packages (this app runs OCR_GPU=False).
# Installing the CPU wheel first keeps memory/disk usage low enough for
# Streamlit Community Cloud's free tier.
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision
easyocr>=1.7
spacy==3.7.5
Pillow>=10.0
numpy<2.0
# en_core_web_md isn't on PyPI under a normal name; pip-installable wheel URL
# so Streamlit Community Cloud can install it straight from requirements.txt.
https://github.com/explosion/spacy-models/releases/download/en_core_web_md-3.7.1/en_core_web_md-3.7.1-py3-none-any.whl